import styled from 'styled-components';

export const Container = styled.div`
  + div {
    margin-top: 8px;
  }

  display: flex;
  border-radius: 8px;
  padding: 32px;
  background-color: #e0e0e0;
`;
